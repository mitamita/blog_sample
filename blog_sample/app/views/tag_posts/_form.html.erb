<%= form_for(tag_post) do |f| %>
  <% if tag_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag_post.errors.count, "error") %> prohibited this tag_post from being saved:</h2>

      <ul>
      <% tag_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %>
    <%= f.text_field :post_id %>
  </div>

  <div class="field">
    <%= f.label :tag_id %>
    <%= f.text_field :tag_id %>
  </div>

  <div class="field">
    <%= f.label :tag_id %>
    <%= f.text_field :tag_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
